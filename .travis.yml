sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t hieubert/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true hieubert/react-test npm test

after_success:
  - docker build -t hieubert/multi-client ./client
  - docker build -t hieubert/multi-nginx ./nginx
  - docker build -t hieubert/multi-server ./server
  - docker build -t hieubert/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push hieubert/multi-client
  - docker push hieubert/multi-nginx
  - docker push hieubert/multi-server
  - docker push hieubert/multi-worker
